var color = "negro";

function pinturas() {
  var color = "rosa";

  console.log(color);

  var listones = " el liston es color: " + color;

  console.log(listones);
}

pinturas(color);
console.log(color);

/* 

var = podemos cmabiarle el valor (varaibles globales o dentro de una funcion)

const = no cambia el valor, utilizada en ciclos for o while

let = no se puede cambiar el valor, pero al declararla podemos maniobrarla

*/
